export enum StyleEnum {
  param = 'formula-param',
  formula = 'formula-node',
  active = 'node-actived',
  content = 'formula-content',
}

export enum InputType {
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
}

export const InputTypes = ['radio', 'checkbox'];
