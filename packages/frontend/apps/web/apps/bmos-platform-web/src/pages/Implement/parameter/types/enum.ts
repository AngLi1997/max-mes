export enum ParameterValueType {
  STRING = 'STRING',
  NUMBER = 'NUMBER',
  BOOLEAN = 'BOOLEAN',
  ENUM = 'ENUM',
  JSON = 'JSON',
}

export enum ParameterBusinessType {
  SYSTEM = 'SYSTEM',
  BUSINESS = 'BUSINESS',
}
