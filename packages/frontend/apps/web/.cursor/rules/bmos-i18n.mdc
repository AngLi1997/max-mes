---
description: 
globs: 
alwaysApply: false
---
# BMOS 国际化模块使用指南

## 目录结构

```
packages/i18n/
├── src/
│   ├── locales/        # 语言包文件
│   │   ├── zh-CN/      # 简体中文
│   │   └── en-US/      # 英文
│   ├── types/          # 类型定义
│   └── index.ts        # 入口文件
```

## 核心功能

### 1. 语言包管理
- 支持多语言切换
- 按模块组织语言包
- 动态加载语言包

### 2. 翻译函数
```typescript
import { t } from '@bmos/i18n';
const message = t('key');
```

### 3. 语言包定义
```typescript
// zh-CN.ts
export default {
  module: {
    key: '中文文本'
  }
};

// en-US.ts
export default {
  module: {
    key: 'English Text'
  }
};
```
`注意：此项目中国际化的key直接为中文文本`

## 使用规范

### 1. 语言包组织
- 按模块划分
- 层级结构清晰
- 键名规范统一

### 2. 翻译使用
- 组件中直接导入使用t

### 3. 类型支持
- 语言包类型定义
- 类型安全的翻译键
- IDE 智能提示

## 最佳实践

1. 语言包管理
   - 模块化组织
   - 命名规范统一
   - 及时同步更新

2. 翻译使用
   - 避免硬编码文本
   - 使用类型检查
   - 保持结构一致

3. 性能优化
   - 按需加载
   - 缓存机制
   - 避免重复翻译

## 注意事项

1. 文案规范
   - 保持一致性
   - 考虑语境
   - 注意语序差异

2. 维护更新
   - 及时同步
   - 版本管理
   - 避免遗漏

3. 开发建议
   - 使用 IDE 插件
   - 定期检查完整性
   - 注意性能影响

## 常见问题

1. 翻译不生效
   - 检查语言包是否加载
   - 确认键名是否正确
   - 验证语言设置

2. 类型提示缺失
   - 更新类型定义
   - 检查导入路径
   - 确认 IDE 配置

3. 性能问题
   - 优化语言包大小
   - 使用按需加载
   - 避免频繁切换
