<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.platform.service.factory.mapper.EquipmentStationUserMapper">

    <select id="queryStationNameListByStationId" resultType="java.lang.String">
        SELECT
            CONCAT_WS( '-', t2.user_name, t2.login_name )
        FROM
            bp_equipment_station_user AS t1
            JOIN bp_user AS t2 ON t1.user_id = t2.user_id
        WHERE
            t1.is_deleted = 0
            AND t2.is_deleted = 0
            AND t1.station_id = #{id}
    </select>
</mapper>
