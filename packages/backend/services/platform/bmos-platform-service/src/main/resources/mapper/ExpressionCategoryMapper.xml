<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.platform.service.system.expression.mapper.ExpressionCategoryMapper">
    <delete id="delete">
        update bp_sys_expression_category
        set del_flag = id,
            is_deleted = 1,
        update_time = now(),
        update_by = #{userId}
        where id = #{id}
    </delete>

    <select id="selectChildrenById"
            resultType="com.bmos.platform.service.system.expression.model.ExpressionCategory">
        select * from bp_sys_expression_category where is_deleted = 0 and find_in_set(#{id}, ancestors)
    </select>
    <select id="selectFullTreeNodeVOList"
            resultType="com.bmos.platform.service.system.expression.vo.ExpressionTreeNodeVO">
        SELECT
            id,
            NAME,
            parent_id,
            create_time
        FROM
            bp_sys_expression_category
        WHERE
            is_deleted = 0
    </select>
</mapper>
