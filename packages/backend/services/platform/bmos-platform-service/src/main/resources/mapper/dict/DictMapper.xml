<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.platform.service.dict.mapper.DictMapper">

    <select id="listDict" resultType="com.bmos.platform.service.dict.vo.DictListVO">
        SELECT
            id as id,
            dict_name as dictName,
            dict_code as dictCode,
            state as state
        FROM
            bp_dict
        WHERE
            is_deleted = 0
        <if test="dictName != null and dictName != '' ">
            and dict_name like concat('%',#{dictName},'%')
        </if>
        <if test="dictCode != null and dictCode != '' ">
            and dict_code like concat('%',#{dictCode},'%')
        </if>
        order by create_time desc
    </select>

    <select id="listDictDown" resultType="com.bmos.platform.service.dict.vo.DictVO">
        select
            id as id,
            dict_name as label,
            dict_code as value
        from
             bp_dict
        WHERE
            is_deleted = 0
    </select>

    <delete id="deleteDict">
        update bp_dict
        set del_flag = id,
            is_deleted = 1,
        update_time = now(),
        update_by = #{userId}
        where id = #{id}
    </delete>
</mapper>
