<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.platform.service.material.mapper.MaterialExtendUnitMapper">

    <select id="selectMaterialBoundExtendUnitList"
            resultType="com.bmos.platform.service.material.vo.MaterialBoundExtendUnitListVO">
        select ue.id,ue.extend_unit_name,ue.expression_value,ue.extend_precision,u.unit_name from bp_material_extend_unit m
        join bp_unit_extend ue on m.extend_unit_id = ue.id
        join bp_unit u on ue.unit_id = u.id
        where ue.is_deleted = 0
        and ue.state = true
        and m.material_id = #{materialId}
    </select>
</mapper>
