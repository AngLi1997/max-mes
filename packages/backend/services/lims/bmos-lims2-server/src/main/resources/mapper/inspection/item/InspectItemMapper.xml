<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.lims2.server.inspect.item.mapper.InspectItemMapper">

    <sql id="BASE_COLUM">
        ii.id,
        ii.code,
        ii.name,
        ii.remark,
        ii.is_system,
        ii.create_by,
        ii.update_by,
        ii.create_time,
        ii.update_time,
        ii.is_deleted
    </sql>

    <!-- 检验项目和分析项的ResultMap -->
    <resultMap id="InspectItemWithParameterMap" type="com.bmos.lims2.server.inspect.item.dto.InspectItemWithParameterDTO">
        <id column="id" property="id"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="remark" property="remark"/>
        <result column="is_system" property="isSystem"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_deleted" property="deleted"/>
        <collection property="parameterList" ofType="com.bmos.lims2.server.inspect.item.dto.InspectItemParameterDTO">
            <result column="parameter_id" property="inspectParameterId"/>
            <result column="parameter_code" property="code"/>
            <result column="parameter_name" property="name"/>
        </collection>
    </resultMap>

    <select id="selectByParam" resultMap="InspectItemWithParameterMap">
        SELECT
            <include refid="BASE_COLUM" />,
            ip.id AS parameter_id,
            ip.code AS parameter_code,
            ip.name AS parameter_name
        FROM
            lm_inspect_item ii
        LEFT JOIN lm_item_parameter lip ON ii.id = lip.inspect_item_id AND lip.is_deleted = 0
        LEFT JOIN lm_inspect_parameter ip ON lip.inspect_parameter_id = ip.id AND ip.is_deleted = 0
        <where>
            <if test="param.code != null and param.code != ''">
                AND ii.code like CONCAT('%', #{param.code}, '%')
            </if>
            <if test="param.name != null and param.name != ''">
                AND ii.name like CONCAT('%', #{param.name}, '%')
            </if>
            <if test="param.keyword != null and param.keyword != ''">
                AND (ii.code like CONCAT('%', #{param.keyword}, '%') OR ii.name like CONCAT('%', #{param.keyword}, '%'))
            </if>
            <if test="param.parameterName != null and param.parameterName != ''">
                AND (ip.name like CONCAT('%', #{param.parameterName}, '%')
                OR ip.code like CONCAT('%', #{param.parameterName}, '%'))
            </if>
            <if test="param.excludeIdList != null and param.excludeIdList.size > 0" >
                AND ii.id  NOT IN
                <foreach collection="param.excludeIdList" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            AND ii.is_deleted = 0
            AND (ii.is_system IS NULL OR ii.is_system = 0)
        </where>
        ORDER BY ii.create_time DESC
    </select>
    <!-- 仅分页查检验项ID，用于两步分页第一步 -->
    <select id="selectIdsByParam" resultType="java.lang.Long">
        SELECT DISTINCT
        ii.id
        FROM
        lm_inspect_item ii
        <if test="param.parameterName != null and param.parameterName != ''">
            LEFT JOIN lm_item_parameter lip ON ii.id = lip.inspect_item_id AND lip.is_deleted = 0
            LEFT JOIN lm_inspect_parameter ip ON lip.inspect_parameter_id = ip.id AND ip.is_deleted = 0
        </if>
        <where>
            <if test="param.code != null and param.code != ''">
                AND ii.code like CONCAT('%', #{param.code}, '%')
            </if>
            <if test="param.name != null and param.name != ''">
                AND ii.name like CONCAT('%', #{param.name}, '%')
            </if>
            <if test="param.keyword != null and param.keyword != ''">
                AND (ii.code like CONCAT('%', #{param.keyword}, '%') OR ii.name like CONCAT('%', #{param.keyword}, '%'))
            </if>
            <if test="param.parameterName != null and param.parameterName != ''">
                AND (ip.name like CONCAT('%', #{param.parameterName}, '%')
                OR ip.code like CONCAT('%', #{param.parameterName}, '%'))
            </if>
            <if test="param.excludeIdList != null and param.excludeIdList.size > 0" >
                AND ii.id  NOT IN
                <foreach collection="param.excludeIdList" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            AND ii.is_deleted = 0
            AND (ii.is_system IS NULL OR ii.is_system = 0)
        </where>
        ORDER BY ii.code ASC
    </select>

    <!-- 根据ID集合查询检验项详情（包含分析项），不分页 -->
    <select id="selectDetailsByIds" resultMap="InspectItemWithParameterMap">
        SELECT
            <include refid="BASE_COLUM" />,
            ip.id AS parameter_id,
            ip.code AS parameter_code,
            ip.name AS parameter_name
        FROM
            lm_inspect_item ii
        LEFT JOIN lm_item_parameter lip ON ii.id = lip.inspect_item_id AND lip.is_deleted = 0
        LEFT JOIN lm_inspect_parameter ip ON lip.inspect_parameter_id = ip.id AND ip.is_deleted = 0
        <where>
            <if test="ids != null and ids.size > 0">
                AND ii.id IN
                <foreach collection="ids" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            <if test="ids == null or ids.size == 0">
                AND 1 = 0
            </if>
            AND ii.is_deleted = 0
        </where>
        ORDER BY
        FIELD(ii.id,
            <foreach collection="ids" item="id" separator=",">
                #{id}
            </foreach>
        ),
        ip.code ASC
    </select>
</mapper>