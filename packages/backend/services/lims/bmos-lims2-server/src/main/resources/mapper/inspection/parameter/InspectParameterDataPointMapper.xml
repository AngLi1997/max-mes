<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.lims2.server.inspect.parameter.mapper.InspectParameterDataPointMapper">

    <select id="selectByParameterId" resultType="com.bmos.lims2.server.inspect.parameter.entity.InspectParameterDataPoint">
        select *
        from lm_inspect_parameter_data_point
        where parameter_id = #{parameterId}
          and is_deleted = 0
    </select>

    <update id="deleteByParameterId">
        update lm_inspect_parameter_data_point
        set is_deleted = 1
        where parameter_id = #{parameterId}
    </update>

    <select id="existByName" resultType="boolean">
        select count(1) > 0
        from lm_inspect_parameter_data_point
        where parameter_id = #{parameterId}
          and name = #{name}
          and is_deleted = 0
    </select>

</mapper> 