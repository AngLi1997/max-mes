<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.lims2.server.stability.plan.mapper.StabilityInspectPlanMapper">

    <select id="selectByCode" resultType="com.bmos.lims2.server.stability.plan.entity.StabilityInspectPlan">
        SELECT *
        FROM lm_stability_inspect_plan
        WHERE code = #{code}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="selectMaxSeq" resultType="java.lang.Long">
        SELECT MAX(CAST(SUBSTRING(code, 2) AS UNSIGNED))
        FROM lm_stability_inspect_plan
        WHERE code REGEXP '^T[0-9]{9}$'
    </select>

</mapper>
