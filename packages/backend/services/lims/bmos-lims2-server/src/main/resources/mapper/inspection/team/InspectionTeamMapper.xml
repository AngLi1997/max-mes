<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.lims2.server.inspect.team.mapper.InspectionTeamMapper">
    <select id="queryPage" resultType="com.bmos.lims2.server.inspect.team.dto.InspectionTeamDTO">
        select distinct
        t1.id,
        t1.code,
        t1.name,
        t1.description,
        t1.number,
        t1.status,
        t1.create_time
        from lm_inspection_team t1
        left join lm_resource_permission t2 on t1.id = t2.resource_id
        <where>
            <if test="code != null and code != ''">
                and t1.code like concat('%', #{code}, '%')
            </if>
            <if test="name != null and name != ''">
                and t1.name like concat('%', #{name}, '%')
            </if>
            <if test="deptIds != null and deptIds.size() > 0">
                and t2.dept_id in
                <foreach collection="deptIds" open="(" separator="," close=")" item="deptId">
                    #{deptId}
                </foreach>
            </if>
        </where>
        order by t1.create_time desc
    </select>
    <select id="queryList" resultType="com.bmos.lims2.server.inspect.team.dto.InspectionTeamDTO">
        select distinct
        t1.id,
        t1.code,
        t1.name,
        t1.description,
        t1.number,
        t1.status,
        t1.create_time
        from lm_inspection_team t1
        left join lm_resource_permission t2 on t1.id = t2.resource_id
        <where>
            t1.status=1
            <if test="code != null and code != ''">
                and t1.code like concat('%', #{code}, '%')
            </if>
            <if test="name != null and name != ''">
                and t1.name like concat('%', #{name}, '%')
            </if>
            <if test="deptIds != null and deptIds.size() > 0">
                and t2.dept_id in
                <foreach collection="deptIds" open="(" separator="," close=")" item="deptId">
                    #{deptId}
                </foreach>
            </if>
        </where>
        order by t1.create_time desc
    </select>

</mapper>