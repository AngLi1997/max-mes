<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.mes.service.weigh.centre.config.mapper.IWeighCentreCategoryMapper">

    <select id="listAllChildren" resultType="com.bmos.mes.service.weigh.centre.config.model.WeighCentreCategory">
        select *
        from bm_weigh_centre_category
        where bm_weigh_centre_category.is_deleted = 0
        <if test="parentId != null">
            and id_path like concat((select id_path from bm_weigh_centre_category where id = #{parentId}), ',', '%')
        </if>
    </select>

    <select id="getNamePath" resultType="com.bmos.mes.service.weigh.centre.config.vo.WeighCentreCategoryPath">
        select b1.id,
               b1.name,
               b1.id_path,
               group_concat(b2.name order by b2.id separator '/') as name_path
        from bm_weigh_centre_category b1
                 left join bm_weigh_centre_category b2 on find_in_set(b2.id, b1.id_path) and b2.is_deleted = 0
        where b1.is_deleted = 0 and b1.id in
            <foreach collection="ids" separator="," open="(" close=")" item="id">
                #{id}
            </foreach>
        group by b1.id
    </select>
</mapper>