<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.mes.service.plan.rule.mapper.CodeRuleMapper">
    <delete id="deleteByCodeRuleCode">
        delete from bm_product_plan_code_rule where code_rule_code = #{codeRuleCode}
    </delete>

    <delete id="deleteByProcessIdAndType">
        delete from bm_product_plan_code_rule where type = #{typeEnum} and process_id in
        <foreach collection="processIds" open="(" separator="," close=")" item="item">
            #{item}
        </foreach>
    </delete>

    <select id="page" resultType="com.bmos.mes.service.plan.rule.vo.CodeRulePageVO">
        select
            t1.id as processId,
            t1.name as processName,
            t2.id as productId,
            t2.name as productName,
            t2.merge_code as productCode,
            t3.id,
            t3.code_rule_code,
            t3.code_rule_name
        from bm_process t1
        left join bm_material t2 on t1.product_id = t2.id
        left join bm_product_plan_code_rule t3 on t1.id = t3.process_id
        <if test="type != null and type != '' ">
            and t3.type = #{type}
        </if>
        <where>
            <if test="processName != null and processName != '' ">
                and t1.name like concat('%',#{processName},'%')
            </if>
            <if test="productName != null and productName != '' ">
                and t2.name like concat('%',#{productName},'%')
            </if>
            <if test="productCode != null and productCode != '' ">
                and t2.merge_code like concat('%',#{productCode},'%')
            </if>
        </where>
        order by t1.create_time desc
    </select>
</mapper>
