<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.mes.service.process.mapper.ProcessRelationMapper">

    <select id="selectDetailListByProcessId"
            resultType="com.bmos.mes.service.process.vo.ProcessRelationDetailVO">
        select
        t1.relation_process_id as processId,
        t2.name as processName
        from bm_process_relation t1
        left join bm_process t2 on t1.relation_process_id = t2.id
        where t1.process_id = #{processId}
    </select>
</mapper>
