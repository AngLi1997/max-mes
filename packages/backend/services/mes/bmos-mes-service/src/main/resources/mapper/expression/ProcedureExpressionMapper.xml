<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.mes.service.process.mapper.task.ProcedureExpressionMapper">

    <select id="getConfigByModelId" resultType="java.lang.String">
        SELECT
            t2.condition_details
        FROM
            bm_procedure_expression AS t1
            JOIN bm_procedure_condition AS t2 ON t1.id = t2.expression_id
        WHERE
            t1.is_deleted = 0
            and t2.is_deleted = 0
            and t1.expression_node_type =#{nodeType}
            and t1.procedure_model_id in
        <foreach collection="procedureModelIds" separator="," open="(" close=")" item="procedureModelId">
            #{procedureModelId}
        </foreach>
    </select>

</mapper>
