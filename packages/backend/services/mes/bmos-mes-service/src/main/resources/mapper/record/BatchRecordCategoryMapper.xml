<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.mes.service.record.mapper.BatchRecordCategoryMapper">

    <select id="listCategory" resultType="com.bmos.mes.service.record.model.BatchRecordCategory">
        SELECT
            id,
            name,
            parent_id,
            create_time
        FROM
            bm_batch_record_category
        WHERE
            is_deleted = 0
        order by sort asc
    </select>
    
    <delete id="deleteCategory">
         update bm_batch_record_category
        set del_flag = id,
            is_deleted = 1,
        update_time = now(),
        update_by = #{userId}
        where id = #{id}
    </delete>
</mapper>
