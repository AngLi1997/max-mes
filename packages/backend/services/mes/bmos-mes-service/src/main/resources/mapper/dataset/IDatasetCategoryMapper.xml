<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.mes.service.dataset.mapper.IDatasetCategoryMapper">

    <select id="listAllChildren" resultType="com.bmos.mes.service.dataset.model.DatasetCategory">
        select *
        from bm_dataset_category
        where bm_dataset_category.is_deleted = 0
        <if test="parentId != null">
            and id_path like concat((select id_path from bm_dataset_category where id = #{parentId}), ',', '%')
        </if>
    </select>
</mapper>