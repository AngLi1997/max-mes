<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.mes.service.process.mapper.ProcessBatchRecordRelationMapper">

    <select id="existNonEnableBatchRecord" resultType="java.lang.Long">
        select count(*)
        from bm_process_batch_record t1
        join bm_batch_record_version t2 on t1.batch_record_version_id = t2.id and t2.is_deleted = 0
        where t1.process_version_id = #{processVersionId}
        and t2.state &lt;&gt; #{state}
        limit 1
    </select>
    <select id="getByRecordVersionIds" resultType="java.lang.Long">
        select
            process_version_id
        from bm_process_batch_record
        where batch_record_version_id in
        <foreach collection="batchRecordVersionIds" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>
</mapper>
