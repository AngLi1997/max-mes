<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bmos.mes.service.lotrelease.template.mapper.ILotReleaseTemplateMapper">

    <select id="queryPage" resultType="com.bmos.mes.service.lotrelease.template.model.LotReleaseTemplate">
        select distinct bm_lot_release_template.*
        from bm_lot_release_template
        left join bm_resource_permission on bm_lot_release_template.id = bm_resource_permission.resource_id
        <where>
            bm_lot_release_template.is_deleted = 0
            <if test="pageQuery.name != null and pageQuery.name != ''">
                and bm_lot_release_template.name like concat('%', #{pageQuery.name}, '%')
            </if>
            <if test="categoryIds != null and categoryIds.size() != 0">
                and bm_lot_release_template.category_id in
                <foreach collection="categoryIds" item="id" separator="," open="(" close=")">
                    #{id}
                </foreach>
            </if>
            <if test="categoryIds == null or categoryIds.size() == 0">
                and 1 = 0
            </if>
            <if test="deptIds != null and deptIds.size() > 0">
                and bm_resource_permission.dept_id in
                <foreach collection="deptIds" open="(" separator="," close=")" item="deptId">
                    #{deptId}
                </foreach>
            </if>
            <if test="deptIds == null or deptIds.size() == 0">
                and 1 = 0
            </if>
        </where>
        order by create_time
    </select>
    <select id="selectAuthByDeptIdList" resultType="java.lang.Long">
        select
            bm_lot_release_template.id
        from bm_lot_release_template
                 left join bm_resource_permission on bm_lot_release_template.id = bm_resource_permission.resource_id
        where
            bm_lot_release_template.is_deleted = 0
            AND bm_resource_permission.dept_id in
        <foreach collection="deptIdList" item="deptId" open="(" separator="," close=")">
            #{deptId}
        </foreach>
    </select>

</mapper>